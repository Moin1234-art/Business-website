<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.baramulla.evacueeregister">

    <!-- One permission only, and it is asked for the first time the clerk
         presses Capture Location, not at install. Geo-tagging every evacuee
         property is a departmental requirement, and the reading is stored on
         the property record like any other field.

         Notably absent: INTERNET. The register never uses the network. Cloud
         sync goes through a file the clerk picks with the system picker, so the
         Drive or OneDrive app does the carrying. Backups are written through
         the same picker, which needs no storage permission either. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-feature android:name="android.hardware.location.gps" android:required="false" />

    <application
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:theme="@style/AppTheme"
        android:allowBackup="true"
        android:hardwareAccelerated="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:windowSoftInputMode="adjustResize"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
